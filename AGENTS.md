# cnubot 작업 지침

## 카카오 메시지와 스킬 API 연결

- 이 프로젝트는 사용자가 카카오 챗봇 관리자에서 발화(messageText)를 특정 POST 스킬 URL에 직접 매핑하는 방식으로 운영한다.
- 사용자 직접 입력과 응답 버튼의 `action: message` / `messageText`는 등록된 발화를 통해 해당 스킬 API로 연결된다. 서버 내부의 발화 분기만 추가해서 카카오가 그 API를 호출한다고 가정하지 않는다.
- 기능별 POST 라우터에서 기존 `KakaoRequest`를 받고 `userRequest.utterance`를 해석하여 카카오 응답 JSON을 반환한다. 별도의 GET 데이터 API를 기본 사용자 흐름으로 제안하지 않는다.
- 새 기능이나 버튼을 추가하면 반드시 **POST 경로별로 사용자가 등록해야 하는 정확한 메시지 목록**을 제공한다. URL 예시는 `https://example.com`을 사용하며 실제 구현 완료 여부와 등록 가능 여부도 명시한다.
- 버튼의 label과 messageText를 구분한다. messageText는 등록표와 일치시킨다. 가변 ID나 검색어를 포함하는 발화가 자동으로 매핑된다고 가정하지 않는다.
- `extra` 전달, 엔티티/패턴 매칭, fallback 블록, 직접 block 연결이 필요한 경우 기존에 구성되어 있다고 가정하지 말고 별도 설계·검증한다.
- 최상위 메뉴는 학식 / 셔틀 / 학교혜택으로 독립 구성한다. 학교혜택 진입 버튼이나 카드를 학식·셔틀 응답에 섞지 않는다.
- 상세 규칙과 등록표는 [docs/kakao-message-routing.md](docs/kakao-message-routing.md)를 읽고 변경 시 함께 갱신한다.

## 공개 저장소 정보 보호

- 이 저장소는 public이다. 실제 운영 IP, 비공개 호스트명, SSH 접속 별칭, 계정명, 서버 절대 경로, 인증정보 및 사용자 식별 데이터를 코드·문서·예제·커밋 메시지에 기록하지 않는다.
- API 등록표에는 상대 경로 또는 `https://example.com` 기반 예시만 쓴다. 실제 주소는 저장소 밖의 비공개 운영 설정에서 관리하고 사용자가 등록 시 대입한다.
- 변경 전후에 추가한 내용의 민감정보를 확인한다. 검사 결과에 민감한 원문을 출력하지 않는다.

## 기존 작업 보존

- 배포 서버의 미커밋 변경은 사용자 작업으로 취급한다. 관련 없는 변경을 덮어쓰거나 되돌리지 않는다.
